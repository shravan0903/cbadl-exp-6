name: CI/CD Pipeline - shravan Experiment 6

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build Docker Image
      run: docker build -t shravan/exp6-app .

    - name: Run Test
      run: docker run shravan/exp6-app

    - name: Push Docker Image
      run: docker push shravan/exp6-app
